@using Microsoft.AspNet.Identity

<header class="header white-bg">
    @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

    }

    <div class="toggle-nav">
        <div class="icon-reorder tooltips" data-original-title="Toggle Navigation" data-placement="bottom"></div>
    </div>

    <!--logo start-->
    @Html.ActionLink("Navigator", "Index", "Home", new { @class = "logo" })
    <!--logo end-->

    @*  <div class="nav search-row" id="top_menu">
                <!--  search form start -->
                <ul class="nav top-menu">                    
                    <li>
                        <form class="navbar-form">
                            <input class="form-control" placeholder="Search" type="text">
                        </form>
                    </li>                    
                </ul>
                <!--  search form end -->
            </div>*@
    <div class="top-nav notification-row">
        <!-- notificatoin dropdown start-->
        <ul class="nav pull-right top-menu">
            <!-- user login dropdown start-->
            <li class="dropdown">
                <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                    <span class="profile-ava">
                    </span>
                    <span class="username">@User.Identity.GetUserName()</span>
                    @*<b class="caret"></b>*@
                </a>
               @* <ul class="dropdown-menu extended logout">
                    <div class="log-arrow-up"></div>
                    <li class="eborder-top">
                        <a href="@Url.Action("Manage","Account")"><i class="icon_profile"></i>My Profile</a>
                    </li>
                    <li>
                        <a href="javascript:document.getElementById('logoutForm').submit()"><i class="icon_key_alt"></i>Log Out</a>
                    </li>
                </ul>*@
            </li>
            <!-- user login dropdown end -->
        </ul>
    </div>
</header>
